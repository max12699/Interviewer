import React from 'react'

/**
 * Renders the Dashboard page.
 *
 * @returns {JSX.Element} The rendered Dashboard element containing a root div with the text "Dashboard".
 */
function Dashboard() {
  return (
    <div>Dashboard</div>
  )
}

export default Dashboard